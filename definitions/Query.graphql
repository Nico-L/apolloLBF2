type Query {
  _empty: String
}

extend type Query {
  effaceImage(imageId: String!): EffaceImagePayload
}

extend type Query {
  clSignature(toSign: String!): SignaturePayload
}

extend type Query {
    siteLogin: siteLoginPayload
}

extend type Query {
    atelier(id: String!, token: String!): atelierPayload
}

extend type Query {
    atelierNbPlacesRestantes(id: String!, token: String!): atelierNbPlacesRestantesPayload
}